<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Keyword Ranking Dashboard</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.7.0/chart.min.js"></script>
    <style>
      .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
      }
      .chart-container {
        position: relative;
        height: 400px;
        width: 100%;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
      }
      th,
      td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
      }
      th {
        background-color: #f2f2f2;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Keyword Ranking Dashboard</h1>

      <div class="chart-container">
        <canvas id="rankingChart"></canvas>
      </div>

      <table>
        <thead>
          <tr>
            <th>Keyword</th>
            <th>Ranking</th>
          </tr>
        </thead>
        <tbody>
          <% if @keyword_results.present? %> <% @keyword_results.each do
          |result| %>
          <tr>
            <td><%= result[:keyword] %></td>
            <td><%= result[:ranking] %></td>
          </tr>
          <% end %> <% else %>
          <tr>
            <td colspan="2">No results found.</td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <script>
      var ctx = document.getElementById('rankingChart').getContext('2d');
      var chart = new Chart(ctx, {
          type: 'bar',
          data: {
              labels: <%= raw @keyword_results.map { |r| r[:keyword] }.to_json %>,
              datasets: [{
                  label: 'Average Ranking',
                  data: <%= raw @keyword_results.map { |r| r[:ranking] }.to_json %>,
                  backgroundColor: 'rgba(75, 192, 192, 0.6)',
                  borderColor: 'rgba(75, 192, 192, 1)',
                  borderWidth: 1
              }]
          },
          options: {
              scales: {
                  y: {
                      reverse: true,
                      beginAtZero: true
                  }
              }
          }
      });
    </script>
  </body>
</html>
